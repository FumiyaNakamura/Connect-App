<div class="row">
  <h1 class="underline-common col-md-offset-5">ユーザー一覧</h1>
</div>

<div class="row">  
  <div class="col-md-offset-4">
    <%= will_paginate %>
      <ul>
        <% @users.each do |user| %>
          <p>
            <% if user.image? %>
              <%= image_tag user.image.url, class: "icon-size-100px" %>
            <% else %>
              <%= image_tag "default.jpg", class: "icon-size-100px" %>
            <% end %>
            <%= link_to user.name, user %>
          </p>
        <% end %>
      </ul>
    <%= will_paginate %>
  </div>
</div>