<% provide(:url, user_path(@user)) %>
<% provide(:button_text, "変更を保存する") %>

<h1>ユーザー編集</h1>

<h1>
    <%= image_tag @user.image.url if @user.image? %>
    <%= @user.name %>
</h1>

<%= render "layouts/form" %>